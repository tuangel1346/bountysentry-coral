# CoralOS config — Agent Economy track.
#
# coral-server runs as a PURE MCP coordinator here: NO [wallet] section.
# Settlement happens agent-side via the Anchor escrow client (@solana/web3.js + @coral-xyz/anchor in
# seller/buyer escrow.ts), so coral-server never touches a keypair and stock coral-server:latest works as-is.
#
# Mounted at: /config/coral.toml   (see docker-compose.yml)
# Agent sources mounted at: /agents/*  (the coral-agents/ folder)

[auth]
keys = ["dev"]

[network]
allowAnyHost = true

[registry]
# Discover buyer-agent, seller-agent, and the seller personas (cheap/premium/lazy/worldcup) from /agents.
localAgents = ["/agents/*"]
includeCoralHomeAgents = true
localAgentRescanTimer = "5s"

[docker]
# Native Linux containers reach the host through Docker's default bridge gateway.
address = "172.17.0.1"
